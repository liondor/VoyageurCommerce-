#include "Solveur.h"

Solveur::Solveur()
{
    //ctor
}

Solveur::~Solveur()
{
    //dtor
}

Solveur::BnB(int[][] graphe, int ptsDepart)
{/*
    int max = 999;
    Arraylist openList= new ArrayList();
    Arraylist currentlyExploring= new ArrayList();
    Node origin = new Node(ptsDepart);
    currentlyExploring.addAll(origin.neighbor());
    best=selectBestChild(currentlyExploring)
    currentlyExploring.remove(best);
    if(best.isSolution)
    {
        max = best.getValue
    }

    return best;


*/
}

